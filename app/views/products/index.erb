<h1>All Products</h1>

<% @products.each do |product| %>
  <h3><%= @product.name %></h3>
  <img src="<%= @product.image_url %>" alt="<%= @product.name %>">
  <%= @product.rating %>
  <%= @product.description %>

<% end %>
